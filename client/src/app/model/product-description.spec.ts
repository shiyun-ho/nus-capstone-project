import { ProductDescription } from './product-description';

describe('ProductDescription', () => {
  it('should create an instance', () => {
    expect(new ProductDescription()).toBeTruthy();
  });
});
